import styled from 'styled-components';

const sectionContainer = styled.div`
  margin-top: 32px;
  padding: 0 24px;
`;
const sectionTitle = styled.p`
  font-size: 24px;
  font-weight: 600;
`;
const sectionDescription = styled.p`
  margin-top: 8px;
  font-size: 18px;
  font-weight: 400;
`;
const highlight = styled.p`
  font-weight: 700;
`;

export {sectionContainer, sectionTitle, sectionDescription, highlight};
